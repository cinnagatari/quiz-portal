import { loadDB, version } from "./loadDB";
import idb from "../utils/indexedDB";
import { db, firebase } from "../utils/firebase";

const student = {
    add: async function addStudent(newStudent) {

    },
    edit: async function editStudent(newStudent, original) {

    },
    delete: async function deleteStudent(original) {

    }
}